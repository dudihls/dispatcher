import styled from "styled-components";

interface CardProps  {
    img: string,
    content:string,
    date: string,
    header: string,
    source: string,
}

export const StyledCard = styled.div<CardProps>`
    
    
`